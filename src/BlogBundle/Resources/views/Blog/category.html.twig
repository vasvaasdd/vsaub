Blog category: {{ category }} | <a href="{{ url('blog_index') }}">BACK TO INDEX</a> | <a href="{{ url('blog_category') }}">BACK TO CATEGORIES</a>

<br/>

{% for category in categories %}
<a href="{{ url('blog_category', {'category': category.name}) }}">{{ category.name }}<a/><br/>
{% endfor %}

{% for post in posts %}
<a href="{{ url('blog_post', {'post_title': post.title}) }}">{{ post.title }}<a/><br/>
{% endfor %}
